/**
 * Changelog "current tip" — new entries since last freeze land here.
 * When this file grows beyond ~7KB, freeze: rename to c{next}.js + reset to [].
 * Entries are newest-first within file.
 */
export default [
  {
    date: '2026-05-06',
    title: '（preview）改善排版 toggle — scheme3 + Phase 1 mixed priority + Phase 3f L1 retry 三件組',
    items: [
      '**緣由**：使用者實測 S1 / S2 / S3 對照後選定 S3（依 L4 編號排），並要求把 S3 跟 Phase 1 mixed priority + L1 retry 合成一顆 toggle 方便 A/B 比較。',
      '**改善內容**：(1) scheme3 columnAssign — 改用 topological + L4 walk 演算法：按 L4 sortKey 順序走訪、`col[t] = max(predecessors.col + 1, l4Bound)`，l4Bound 對 parallel sibling 允許同 col、對非 sibling 強制 +1。同時滿足 (a) parallel siblings 連續 contiguous 時同 col 對齊、(b) 中間有孤立任務時嚴格 L4 順序、(c) compact 無空白 col。(2) Phase 1 mixed priority — 4-pass fallback：未被 sibling 用 + 路徑無障礙 + rule-1 OK → 路徑無障礙 → 未被 sibling 用 → priorities[0]。(3) Phase 3f L1 retry — 走完所有 phase 後對紅線連線試 16 種 (exit, entry) 組合、挑第一個 rule 1 + rule 2 都過的。',
      '**isPathClear helper**：`corridor.js` 新增、預測路徑 cells（top→top corridor / right→left Z 形 / top→left L 形 等）+ rule 1 port-mix 檢核。Phase 1 + Phase 3f 共用同一 predicate 確保兩階段判定一致。',
      '**Header 改成 boolean toggle**：「改善排版」按鈕，一鍵開三件組 / 全關。per-flow persistent，跟「錯落」獨立 — 兩者可單獨用、合併用、全關，共 4 種測試組合。',
      '**動到的檔案（5 個）**：`src/diagram/layout/corridor.js`（isPathClear + predictPathCells）/ `src/diagram/layout/phase1and2.js`（4-pass fallback）/ `src/diagram/layout/phase3f.js`（新檔，L1 retry）/ `src/diagram/layout/computeLayout.js`（enhancedRouting flag → scheme3 + Phase 1 useMixedPriority + Phase 3f）/ `src/components/FlowEditor/Header.jsx` + `index.jsx`（toggle button）。',
    ],
  },
  {
    date: '2026-05-06',
    title: '（preview）錯落排列 toggle — 奇數泳道右移半格，跨泳道任務交錯',
    items: [
      '**緣由**：使用者：「我希望跨泳道的元件可以錯落排列⋯⋯第二個角色的元件位置會在上一列角色的一半的位置開始⋯⋯這樣連線可以從第一角色的兩個任務中間穿過。」',
      '**v1 預覽範圍（最小可視化）**：加 per-flow `staggerLanes` boolean toggle、Header 多一顆「錯落」按鈕、`computeLayout` 對奇數 row 平移 `COL_W/2 = 92px`。COL_W 不動、routing 障礙檢查不動 — 純視覺先讓使用者評估。',
      '**Toggle 預設 OFF**：既有流程圖外觀不變，使用者主動開啟才生效；狀態存在 flow 物件本身（不是 localStorage 全域），不同流程可獨立設定。',
      '**動到的檔案（3 個）**：`src/diagram/layout/computeLayout.js`（`laneXOffset` array + cx 加 offset + svgWidth 補 maxLaneXOffset）/ `src/components/FlowEditor/Header.jsx`（「錯落」按鈕）/ `src/components/FlowEditor/index.jsx`（toggleStagger + props 傳遞）。',
      '**部署**：`.github/workflows/deploy-preview.yml` 把 main + 此分支併到單一 Pages deploy；preview 在 `https://cjo4m3c.github.io/FlowSprite/preview-stagger-layout/`。',
      '**已知限制 / 後續決策點**：(a) 鄰排任務水平視覺重疊 48px（不同 y 不撞，但看起來有點疊）— 若不滿意可走方案 B，把 COL_W 從 184 拉到 280；(b) routing 障礙檢查仍用 col 整數格，未改成視覺座標 — 平移後 phase3d 可能誤判某些 cross-lane 連線（暫無回歸案例）；(c) 三泳道以上時 lane 0/2 都不平移、只 lane 1 平移，跨多泳道的視覺效果可能不如預期。',
    ],
  },
  {
    date: '2026-05-05',
    title: 'TaskCard 任務名稱 ↔ 元件類型欄位交換：name 拿 Row 2 雙欄寬編輯空間',
    items: [
      '**緣由**：使用者：「把任務名稱欄位跟任務元件欄位交換，這樣使用者才有比較大空間可以編輯任務名稱」。原 layout：Row 1 col 4 是 name input（單欄寬）/ Row 2 col 3+4 跨欄 span 是 element-type select。任務名稱常常很長（含閘道前綴或長描述），擠在單欄不夠。',
      '**改法**：把兩個欄位位置交換。Row 1 col 4 改放 element-type select（select 自帶 dropdown，內容可 truncate）。Row 2 改放 name input + 跨 col 3+4 雙欄寬。Row 2 label 從「元件類型」改成「任務名稱」。',
      '**新 layout**：',
      '  • Row 1：reorder | col2 chip+num | role select | **element-type select** | ▼ ✕',
      '  • Row 2：spacer | spacer | **任務名稱 label** | **name input（col 3+4 雙欄寬）**',
      '  • Row 3：ConnectionSection 不變',
      '**驗證**：`npm run build` 通過。8 種元件 element-type select 在 Row 1 單欄寬可顯示主標題（"L3 流程（子流程調用）" 等長 label 由 native select truncate / 滑動）。',
      '**動到的檔案（2 個）**：`src/components/FlowEditor/TaskCard.jsx`（Row 1 / Row 2 欄位交換 + label 文字改）/ `src/data/changelog/current.js`（本條）。',
    ],
  },
  {
    date: '2026-05-05',
    title: 'TaskCard col 2 重新設計：彩色 chip 在上 + L4 編號在下 + ℹ tooltip 改 CSS group-hover 即時顯示',
    items: [
      '**緣由**：使用者回報 (1) ℹ icon hover 沒反應 — 之前用 `title` 屬性給 native browser tooltip，但 native title 觸發延遲長、不同瀏覽器對 `\\n` 行為不一致、12px 細字命中區小 (2) 元件名稱應該在編號上方、配合區塊色塊做出明顯樣式。',
      '**Fix 1（ℹ tooltip）**：title 屬性改成 Tailwind `group / group-hover` CSS popover — 即時觸發、深色背景 + 白字、`whitespace-pre-wrap` 支援多行、固定 280px 寬度。內含粗體元件名 / 描述 / 任務關聯說明三段，hover ℹ 立即顯示在下方、moveaway 立即消失。`pointer-events-none` 避免 popover 自己擋住 hover。',
      '**Fix 2（chip 上 + 編號下）**：col 2 從 stacked「mono num 上 / 灰字 label 下」翻轉成「彩色 chip 上 / mono num 下」。新增 `KIND_BADGE` 色票 — 任務 slate / 互動灰白 / 排他 amber / 並行 綠 / 包容 黃 / 子流程 紫 / 開始 綠 / 結束 紅，呼應流程圖 + CONN_BADGE 既有配色。chip 用 `px-1.5 py-0.5 rounded text-xs font-bold` 浮在 row bg 之上，視覺層次清楚。',
      '**順帶**：拿掉 TaskCard 根 div 的 `overflow-hidden` — group-hover popover 用 absolute 定位、原本會被 overflow-hidden 截掉。`rounded-lg` 圓角下卡片內容沒有 bg 會超出，移除 overflow-hidden 視覺無 regression。',
      '**驗證**：`npm run build` 通過。8 種元件 kind 各對應 chip 配色 + tooltip 內容。`group-hover` 機制比 native title 在 Chrome / Firefox / Safari 行為一致。',
      '**動到的檔案（2 個）**：`src/components/FlowEditor/TaskCard.jsx`（KIND_BADGE 新增 + col 2 layout 翻轉 + tooltip 改 CSS popover + 移除 overflow-hidden）/ `src/data/changelog/current.js`（本條）。',
    ],
  },
  {
    date: '2026-05-05',
    title: '同 lane skip-connection routing：偵測上 / 下 corridor 鄰近 lane 任務、走較乾淨那側、預設上方',
    items: [
      '**緣由**：使用者決策 7 情境 C — 同 lane skip-connection（A1 → A3 中間有 A2）目前由 Phase 3c 自動繞 corridor，但選擇 top 或 bottom 只看「port-mix」/「corridor 已被佔用」結構性檢核，沒看「鄰近 lane 是否有任務」這個視覺乾淨度判準。使用者：「偵測上下方式分有任務或其他元件，走沒有元件的那一側（沒有的話就預設走上方）」。',
      '**改法（`src/diagram/layout/phase3bc.js`）**：',
      '  • 加 `adjacentLaneHasTask(taskAt, adjRow, minCol, maxCol)` helper — 掃 adjRow 在 (minCol, maxCol) 區間是否有任何 task。adjRow < 0 視為空（topmost lane 上方無 lane）；taskAt 對 out-of-range row 回 undefined。',
      '  • Phase 3b（同 lane backward）跟 Phase 3c（同 lane forward dc>1）兩處都加新判準：當 top 結構性檢核都過（!topMix && !topCross）但鄰近 lane 上方 (fr-1) 有任務、下方 (fr+1) 沒有 + bottom 沒 mix → 改走 bottom corridor。',
      '  • 兩側都空 → 維持 top（使用者指定 default）',
      '  • 兩側都有 → 維持 top（同 default 規則；不增加額外 fallback）',
      '  • 結構性 topBad（mix/cross）→ 沿用既有「bottom mix-free 才用 bottom」，不被新判準干擾',
      '**為什麼安全**：新判準只在「結構上 top OK」時介入，不會破壞 rule 1（port-mix）/ rule 2（線跨任務）的硬性檢核。最壞情況 fallback 還是回 top。',
      '**驗證**：`npm run build` 通過。Phase 3c 邏輯本身已有單元 case（PR-1 拆檔時測過）。新增 case 由 user UI 實測驗證視覺改善。',
      '**動到的檔案（2 個）**：`src/diagram/layout/phase3bc.js`（adjacentLaneHasTask helper + Phase 3b/3c 加 cleaner-side preference）/ `src/data/changelog/current.js`（本條）。',
      '**對應使用者決策 7 情境 C 收尾完成**。決策 1-5 layout idx-driven 已 PR #177 落地、本條落地決策 7-C，整套「自動連線優化、閘道避開」backlog 條目可從待辦移除。',
    ],
  },
  {
    date: '2026-05-05',
    title: '編輯器再優化：col 2 stacked 編號+元件名+ℹ tooltip / 移除 4 個灰色 inline 說明 / pl-1 對齊修 / 流程斷點全面退役',
    items: [
      '**緣由**：使用者三項要求 — (1) 移除底部灰色 inline 說明、改用 ℹ icon + hover 顯示「元件功能說明 + 任務關聯說明完整文字」 (2) Row 2「元件類型」label 跟 Row 1 編號 / Row 3 ConnectionSection labels 對齊（去 pl-1） (3) 編輯區塊要看到編號**也**要看到元件名稱（例「任務」「包容閘道」「外部互動」）。順帶把已淡化的 breakpoint 流程斷點全面退役（保留 storage / model 相容、UI 入口砍光）。',
      '**Fix 1（col 2 stacked + ℹ tooltip）**：col 2 改 `flex flex-col`，上行 mono L4 編號、下行短名稱（任務 / 排他閘道 / 並行閘道 / 包容閘道 / 子流程 / 開始事件 / 結束事件 / 外部互動）+ 一個 ℹ icon。Hover ℹ → `title` 屬性顯示「[元件名] / 描述 / 任務關聯說明：[formatConnection 衍生字串]」。8 種 kind 各有 description 對應原本散在 ConnectionSection 的 4 個灰色註解 + 額外補完。',
      '**Fix 2（移除 4 個灰色 inline 說明）**：`ConnectionSection.jsx` 拿掉 (a) subprocess 「ℹ 此任務將以 L3 活動元件...」 (b) parallel-branch 「ℹ AND 並行閘道...」 (c) inclusive-branch 「ℹ OR 包容閘道...」 (d) loop-return 「ℹ 迴圈返回...」。內容已搬到 TaskCard 的 ℹ tooltip。',
      '**Fix 3（Row 2 對齊）**：`TaskCard.jsx` Row 2 col 2「元件類型」label 拿掉 `pl-1`（4px padding-left），現在跟 Row 1 col 2（mono 編號）+ Row 3 col 2（ConnectionSection labels「下一步 →」「條件 1 →」等）三排同 x 起點。',
      '**流程斷點 breakpoint 全面退役（UI 層）**：(a) `ELEMENT_TYPES` 沒列；TaskCard 拿掉 `currentKind === "breakpoint" ? [...ELEMENT_TYPES, breakpoint legacy] : ELEMENT_TYPES` 的條件分支，現在永遠用 `ELEMENT_TYPES` 不再 surface breakpoint 選項 (b) `ConnectionSection.jsx` 移除 `if (ct === "breakpoint")` block (c) `elementTypes.js detectElementKind`：`task.type === "end"` 直接 return `"end"`（不再分 end / breakpoint）；`makeTypeChange` 移除 breakpoint case，使用者點 ELEMENT_TYPES 沒 breakpoint 也選不到 (d) `TaskCard.nameOptional` 從 `start || end || breakpoint` 砍成 `start || end` (e) **保留** `taskDefs.js` CONN_BADGE / CONN_ROW_BG 的 breakpoint entry、`storage.js` 的 connectionType 處理、`formatConnection.js` 的 breakpoint render 等模型層 — 既有 localStorage / Excel 含 breakpoint 的 task 仍正常顯示為「結束事件」。',
      '**Excel 匯入偵測「流程斷點」字樣**：`collectCrossCheckWarnings` 新增 row 掃描 — D 欄（任務名稱）/ E 欄（任務重點說明）/ I 欄（任務關聯說明）任一含「流程斷點」字樣 → warning「列 N 含『流程斷點』字樣 — 此元件已停用，請改用結束事件 -99 或調整流程設計後重新匯入」。Warning 不擋匯入（讓使用者自己修）。',
      '**驗證**：`npm run build` 通過。功能驗證點：(a) col 2 雙行 layout 正確、ℹ hover 顯示 tooltip (b) Row 2「元件類型」跟其他 row 同 x 對齊 (c) ConnectionSection 4 個灰色說明全消失 (d) ELEMENT_TYPES 下拉選單沒「流程斷點」 (e) 既有 breakpoint task 進編輯器顯示為結束事件、不破壞流程 (f) 匯入 Excel 含「流程斷點」row → 跳警示。',
      '**動到的檔案（5 個）**：`src/components/FlowEditor/TaskCard.jsx`（col 2 stacked + ℹ + 對齊修 + breakpoint 移除）/ `src/components/ConnectionSection.jsx`（4 個灰色 + breakpoint case 移除）/ `src/utils/elementTypes.js`（detectElementKind / makeTypeChange 移 breakpoint）/ `src/utils/excelImport.js`（流程斷點 warning）/ `src/data/changelog/current.js`（本條）。',
    ],
  },
  {
    date: '2026-05-05',
    title: '編輯器 TaskCard 三項優化：外部互動底色獨立 / 全元件類型顯示編號 / 移除底部關聯說明 preview',
    items: [
      '**緣由**：使用者要求 (1) 外部互動跟一般任務在編輯器中目前共用 sequence 底色 `#FAFAFA`，視覺上分不出 — 應該像閘道一樣有獨立底色 (2) col 2 的編號目前只在 sequence 顯示，閘道 / 開始 / 結束 / 子流程被彩色 badge 蓋掉 — 希望全部都顯示 L4 編號 (3) Card 底部的「關聯說明」preview 多餘（同樣文字已在 FlowTable / Excel / drawio 都有），請移除。',
      '**Fix 1（外部互動底色）**：`TaskCard.jsx` rowBg 邏輯加分支 — `ct === "sequence" && task.shapeType === "interaction"` → 套 `INTERACTION_ROW_BG = "#F1F5F9"`（slate-100，呼應流程圖 `#A0A0A0` 互動填色但較淺）。其他元件繼續走 `CONN_ROW_BG[ct]`（閘道 / 開始 / 結束 / l3activity 各自不同色）。一般任務維持 `#FAFAFA`。',
      '**Fix 2（全元件顯示編號）**：col 2 從原本「sequence 顯示 num / 其他顯示彩色 badge」改成統一「`text-sm font-mono text-gray-600 font-semibold`」直接顯示 `displayLabels[task.id]`。彩色 badge 移除（背景色已能傳達元件類別、與閘道 badge 標籤資訊重複）。閘道 / 開始 / 結束 / 子流程 / 外部互動現在全部能看到完整 L4 編號（`1-1-5-2_g` / `1-1-5-0` / `1-1-5-99` / `1-1-5-2_s` / `1-1-5-2_e1`）。',
      '**Fix 3（移除底部 annotation preview）**：拿掉 `formatConnection` import + `annotation` 變數 + 整個底部 preview block（原 line 113-119）。FlowTable 跟 Excel 匯出都仍有同樣文字、不會丟失資訊。TaskCard 視覺更精簡。',
      '**順手清 dead import**：`CONN_BADGE` 不再用，從 `import` 句移除。`badge` 變數整個移除（原本的色彩 badge 邏輯）。',
      '**驗證**：`npm run build` 通過。對所有元件類型確認 col 2 都能讀到正確編號（依賴 `computeDisplayLabels` SOT，不需要新邏輯）。',
      '**動到的檔案（2 個）**：`src/components/FlowEditor/TaskCard.jsx`（rowBg 邏輯 + col 2 重寫 + 移除 annotation preview + dead import 清理）/ `src/data/changelog/current.js`（本條）。',
    ],
  },
  {
    date: '2026-05-05',
    title: '輔助欄位 schema 改版：新增 5 欄 / 改寫 1 個 header / 移除 4 個 separator + FlowTable toolbar 三鍵改版',
    items: [
      '**緣由**：使用者要求調整輔助欄位資訊（保留辨識規則）— 新增 5 個欄位（備註 / SO 釐清狀況 / 找 User 釐清 / 合併場次 / User 釐清狀況）、改寫一個 header（「(1-實體)(2-分配)」→「牽涉實體或分配（1-實體、2-分配）」）、移除原本 4 個視覺 separator 改為 21 欄連續排列。同時要求 FlowTable toolbar 三鍵改順序、改純文字標籤、確認預設值。',
      '**Schema（`utils/auxFieldDefs.js`）**：AUX_FIELDS 從「16 keyed + 4 separator = 20 欄」變「21 keyed = 21 欄」。新欄位 keys：`note` / `soClarifyStatus` / `userClarify` / `mergedSession` / `userClarifyStatus`。既有 keys（executor / system / entityAllocType 等 16 個）保留 — 既有 localStorage 資料 task.meta 不需要 migrate。Excel 範圍從 K~AD 變 K~AE。',
      '**Excel 匯入相容**：buildAuxColMap 走 header mapping，舊 Excel 用「(1-實體)(2-分配)」header 不再被認得（找不到該 header → entityAllocType 欄位空白），但**不會擋下匯入**（容錯設計）。使用者拿舊 Excel 可手動更新 header 文字後重匯入，或直接在 FlowTable 重填。',
      '**FlowTable toolbar 改版**：三顆按鈕由左至右順序「適應內容高度 / 隱藏輔助欄位 / 隱藏L3欄位」。純文字標籤（移除 ⊕ ⊖ ⇕ ▲ ▼ icon）。Active 狀態深藍底白字、Inactive 白底藍邊。預設：適應內容高度 OFF、隱藏輔助欄位 ON、隱藏L3欄位 ON（首次造訪兩鍵 active、一鍵 inactive，後續使用者偏好持續化）。',
      '**文件三件組同步**：(a) `docs/business-spec.md §10`：30 欄 → 31 欄；§10.1 詳細表從「分組四段（K~N / O-P / Q~U / V / W~AA / AB / AC~AD）」改為連續 21 欄表（K~AE 每欄一條） (b) `src/data/helpPanelData.js`：EDITABLE_ACTIONS toggle 條目重寫對應新標籤 + 預設值；EXPORTS Excel 條目從 30→31 欄 (c) `src/utils/excelExport.js` 註解 K~AD → K~AE。',
      '**驗證**：`npm run build` 通過，bundle size +400 bytes（新增 5 個 header 字串）。功能驗證點：(a) 新匯入 Excel 含 21 個 header 全部對到 task.meta (b) 舊 Excel 帶 20 欄 → 19 欄正確匯入、entityAllocType 因 header 變動空白（容錯不報錯） (c) FlowTable 三鍵 toggle / active 樣式 / 預設值正確 (d) Excel 匯出 31 欄 + AB~AE 新欄位內容正確。',
      '**動到的檔案（5 個）**：`src/utils/auxFieldDefs.js`（rewrite AUX_FIELDS）/ `src/components/FlowTable.jsx`（toolbar 三鍵改版）/ `docs/business-spec.md` §10（30→31 欄、表格重寫）/ `src/data/helpPanelData.js`（EDITABLE_ACTIONS + EXPORTS 同步）/ `src/utils/excelExport.js`（doc comment K~AD→K~AE）/ `src/data/changelog/current.js`（本條）。',
    ],
  },
  {
    date: '2026-05-05',
    title: 'PR-D12：Excel 匯入警示加 Excel 列號 + 重生 stored flowAnnotation + FlowEditor 流程上方 banner + 持續化',
    items: [
      '**緣由**：使用者回報 (a) 匯入訊息「`[排他閘道]判斷客戶確認結果」5-1-2-7_g → 5-1-2-6_g`」只有元件名跟編號、沒列號，800 行 Excel 找不到要修哪一列 (b) 想確保 normalize 後 stored data 完全一致（task.flowAnnotation 不殘留 Excel 原始字串） (c) 想要使用者點進流程圖時頂部也能看到匯入時的提醒、可主動 dismiss。',
      '**Step 1（Excel 列號）**：`parseExcelToFlow` 開頭建 `excelRowByL4` map（`l4 → 1-indexed Excel row`）+ `l3OfRow` map（`row → l3Number`）。`normalizeL4Numbers` 收 `excelRowByL4` 參數，每筆 fix 額外帶 `excelRow` 欄位。warning 訊息升級為「`第 547 列「轉換請購訂單」5-3-3-5 → 5-3-3-4`」。Synthetic start/end 沒對應 Excel row 時優雅 fallback 不顯示「第 X 列」。',
      '**Step 2（重生 stored flowAnnotation）**：normalize 完之後跑一遍 `formatConnection(task, tasks, l4Map)` 寫回 `task.flowAnnotation`，讓 stored 字串跟 displayLabels（流程圖、FlowTable、Excel 匯出實際讀的）永遠同步。修掉 c13 留下的 stored vs derived 不一致歷史包袱。Round-trip：匯入→存→reload→匯出 全部對齊。',
      '**Step 3（per-flow `importWarnings` 持續化）**：`flow.importWarnings = []` 加在每個 flow 物件上，parseExcelToFlow 的所有 warning 都 push 到對應 flow（normalize / merge / validation 走 per-flow loop 自然分流；cross-row 的 chain warnings + cross-check warnings 用 regex 抓「第 N 列」→ `l3OfRow[N]` → flow 路由）。flow 存進 localStorage 時 importWarnings 一起持續化（migrateFlow 沒 strip 此欄位）。',
      '**Step 4（FlowEditor 頂部 banner）**：`src/components/FlowEditor/index.jsx` `<main>` 內、DiagramRenderer 上方加琥珀色 banner — 顯示「匯入時自動調整了 N 筆內容」+ 滾動列表 max-h-64 + ✕ 按鈕。dismiss handler 直接 `setLiveFlow + saveFlow` 持續化（繞過 saveAndValidate 不要求使用者 click 儲存按鈕）。下次再開該流程不會再顯示。',
      '**為什麼 dismiss 立即持續化**：使用者預期「✕ 按下去就消失」。若走 patch + hasChanges + 儲存按鈕的常規路徑，使用者要多一步點儲存才真的清。直接 `saveFlow(updated)` 一個字段、零 validation 風險。',
      '**Dashboard 端 importWarnings 行為（不變）**：仍是 session-scoped useState，匯入後立即顯示。新增的 per-flow 持續化只服務「使用者下次開流程時還能看到」這個訴求；Dashboard 的「剛 import 完的 banner」靠 session state 即可，不需要持續化。',
      '**動到的檔案（3 個）**：`src/utils/excelImport.js`（excelRowByL4 map + normalizeL4Numbers 簽名 + per-flow warnings 路由 + regenerate flowAnnotation）/ `src/components/FlowEditor/index.jsx`（saveFlow import + handleDismissImportWarnings + banner JSX）/ `src/data/changelog/current.js`（本條）。`npm run build` 通過。',
    ],
  },
  {
    date: '2026-05-05',
    title: 'PR-D11：D 系列 doc / cleanup 收尾（CLAUDE / README / HANDOVER / backlog 同步、刪未用 SHAPE_TYPES、修 migrateLegacyWtoE typo 註解、關 stale PRs）',
    items: [
      '**緣由**：D 系列 D1–D10 + aux fields A/B/C 共 13 支 PR 連續落地後，文件 / 冗餘碼 audit 找出零散 staleness 跟 dead code。本 PR 一次收齊，避免散在各 follow-up。',
      '**Doc 同步（4 個檔案）**：',
      '  • `CLAUDE.md §10` 規則 4 row：`-0 / -99 / _g* 不顯示` → 補上 `_s*` / `_e*` 對齊 §3 詳細規則',
      '  • `README.md`：(a) 目錄樹 changelog 從 `c01.js…c21.js` → `c01.js…c24.js`（D1 freeze c24） (b) `storage.js` migration 數量從 5 → 6（PR-D10 加 `migrateTypeFromL4Suffix`） (c) 「關鍵檔案」段同步 storage 描述',
      '  • `HANDOVER.md`：(a) 同 README 改 changelog chunk 數 + storage migration 數 (b) §3.1 編號格式表加「外部關係人互動 _e」row、註解從「`_g` 與 `_s` 共用」擴成「`_g` / `_s` / `_e` 共用」 (c) §3.1 加 PR-D10「L4 後綴是 SOT」備註',
      '  • `.claude/backlog.md`：補 D 系列 13 支 PR done log（PR #154 HelpPanel + #155-157 aux fields + #158-168 D1-D10）+ 「後續批次拆檔」size 數據更新（`excelImport.js` 15.5KB → 30KB 標記為 PR-D5+D10 撐爆）',
      '**冗餘碼清理（`utils/taskDefs.js`）**：(a) 刪未用的 `SHAPE_TYPES` 常數匯出（PR #111 後已被 ELEMENT_TYPES 取代，零 importer，c21 changelog 仍提及屬歷史描述不影響） (b) 改 line 38 註解 `storage.migrateLegacyWtoE` typo → `storage.migrateInteractionSuffix`（實際函式名）',
      '**Stale PR 收掉**：(a) PR #83（Dashboard 拆檔，#82 後沒 rebase 過）→ closed (b) PR #125（backlog merge，AF/AH 已 dropped、AJ 已併入）→ closed',
      '**Local branch cleanup**：squash merge 後本地分支沒自動刪，14 個 stale local branches 一次 `git branch -D` 清掉（aux-fields-pr-a/b/c、external-role-pr-d1 ~ d10、update-rules-formatting）。',
      '**動到的檔案（5 個）**：`CLAUDE.md`（1 行）/ `README.md`（3 處）/ `HANDOVER.md`（4 處）/ `.claude/backlog.md`（D 系列 done log + size 更新）/ `src/utils/taskDefs.js`（刪 SHAPE_TYPES + 修註解）/ `src/data/changelog/current.js`（本條）。`npm run build` 通過、bundle size 不變。',
    ],
  },
  {
    date: '2026-05-05',
    title: 'PR-D10：元件類型判斷改為「L4 編號為 SOT」，加跨訊號 cross-check + OR 詞彙容忍清單擴充',
    items: [
      '**緣由**：使用者上傳 Excel 出現「`_g` 編號 + `[包容閘道]` 名稱 + `條件分支至` 任務關聯說明」的三訊號矛盾情境，系統判為 XOR、但名稱寫包容閘道 → 視覺上「辨識錯誤」。Trace 後確認系統現行設計**只看任務關聯說明**做 type 判斷，名稱跟編號後綴都被忽略。經跟使用者討論 8 個情境後決定改為「L4 編號是 SOT、name + body 是輔助訊號」。',
      '**Step 1：L4 編號變主訊號（`utils/excelImport.js buildFlow`）**：新增 `detectKindFromL4(l4Num)` 純函式 — `-0` → start / `-99` → end / `_g\\d*` → gateway / `_s\\d*` → l3activity / 其他 → task。`buildFlow` 改成先看 L4 後綴決定 type，body 詞彙只剩「補閘道子型」(`xor`/`and`/`or`) 一個用途。Body 缺 fork keyword 時 gateway 預設 XOR（decision 2 / B2）。',
      '**Step 2：OR 詞彙容忍清單擴充（`model/connectionFormat.js`）**：`RE_OR_FORK` 跟 `detectGatewayFromText` 加「包含分支至」（BPM 業界同義詞，decision 3）。原有「包容/可能分支至」維持。',
      '**Step 3：cross-check blocking errors（`validateNumbering`，decision 4）**：3 條新檢核 — (a) L4 `_g` + body「調用子流程」→ 衝突 block (b) L4 `_s` + body 任一 fork keyword → 衝突 block (c) L4 `_s` 但 body 缺「調用子流程 X-Y-Z」→ block（沒 L3 編號無法呼叫子流程）。',
      '**Step 4：cross-check warnings（新 `collectCrossCheckWarnings`，decision 2 + 6）**：4 條軟提醒 — (a) L4 `_g` + body 缺 fork keyword → 「預設 XOR」warning (b) name `[XX閘道]` 與 body 詞彙不一致 → 「以 body 為準」warning (c) L4 `-0` + name 沒「開始事件」→ 建議補前綴 (d) L4 `-99` + name 沒「結束事件」→ 建議補前綴。',
      '**Step 5：localStorage migration（`storage.js migrateTypeFromL4Suffix`，decision 1）**：load 時 task.type = "task" 但 L4 後綴另有所指 → 強制改 type 跟 L4 對齊。`_g` → gateway (默 xor)、`_s` → l3activity、`-0` → start、`-99` → end。Idempotent，跟既有 `migrateGatewaySuffix`（type→L4 反方向）共存。',
      '**Step 6：localStorage 載入維持寬鬆（decision 5 / E2+E3）**：不要求 conditions 已填、不要求 body 詞彙必有 — 信任本地 model（task.type / task.gatewayType 是編輯器存的）。Excel 匯入才走嚴格 cross-check。',
      '**Step 7：規格文件（`docs/business-spec.md §3` 新增 SOT 規則段落）**：明確標註元件類型由 L4 後綴決定、name / body 是輔助訊號。',
      '**驗證**：`npm run build` 通過。Sanity test 7 個典型情境（含使用者原 case）→ kind 判定正確、cross-check 該擋的擋、該 warn 的 warn。',
      '**動到的檔案（4 個）**：`src/utils/excelImport.js`（detectKindFromL4 + detectGatewayFromName + collectCrossCheckWarnings + buildFlow 改用 L4-first + validateNumbering 新 3 條 blocking）/ `src/model/connectionFormat.js`（OR 詞彙加「包含分支至」）/ `src/utils/storage.js`（migrateTypeFromL4Suffix）/ `docs/business-spec.md` §3（SOT 規則段落）/ `src/data/changelog/current.js`（本條）。',
    ],
  },
  {
    date: '2026-05-05',
    title: 'PR-D7：修連線解析 regex 漏 `_e` 後綴（造成閘道目標解錯、`_e` 互動連線斷）',
    items: [
      '**緣由**：使用者回報「辨識包容閘道、排他閘道、並行閘道會有錯誤」。Trace 後發現 `model/connectionFormat.js` 的 NUM 系列 regex 只認 `_g` / `_s` 後綴，**`_e` 沒在 regex 裡** — 所有任務關聯說明文字裡的 `_e` 互動編號 reference 解析時都被截成 base 號碼。',
      '**Bug 範圍（regex 3 處）**：(a) `NUM = "([\\\\d.-]+(?:_g\\\\d*|_s\\\\d*)?)"` 餵 RE_SEQUENCE / RE_RETURN / 三個 RE_*_MERGE / RE_LOOP_BACK，共 6 個 regex (b) `NUM_HEAD` 餵 splitForkEntries 解 fork 目標清單（XOR/AND/OR 三種） (c) `RE_LOOP_LEGACY` 兩個 capture group 也漏 `_e`。',
      '**閘道辨識被波及的劇本**：例 `1-1-5-3_g` 任務文字「條件分支至 1-1-5-2_e1（同意）、1-1-5-2_e2（不同意）」 → splitForkEntries 把兩個 `_e` 目標都截成 `1-1-5-2` → dedupe 後 `branchToNumbers = ["1-1-5-2"]`（**少一條分支** + 號碼錯）→ taskByNumber 查 `1-1-5-2` 不到（若該流程無 base 任務）或接到錯誤的 base 任務。**閘道 type 偵測本身仍對**（\'xor\'/\'and\'/\'or\' 用 keyword regex 抓 raw text），但目標解錯讓使用者主觀感受「閘道辨識錯誤」。',
      '**修法**：三處 regex 把 `_g\\d*|_s\\d*` 擴成 `_g\\d*|_s\\d*|_e\\d*`，沒動其他邏輯。Export 端（`formatConnection`）原本就直接讀 `displayLabels[id]` 組字串、不經 regex，所以單向只壞 import — round-trip（export `_e` 任務 → re-import）資訊完整保留。',
      '**驗證**：Node 單元測試三種 fork 含 `_e` 目標 + label + 後接序列流向 → splitForkEntries 正確切出全部 `_e` 互動編號。`npm run build` 通過。功能驗證點：(a) 含 `_e` 目標的 XOR/AND/OR 閘道 → 全部分支正確 wiring (b) 一般任務 `序列流向 X_e1` → 正確接到 `_e` 互動 (c) `迴圈返回至 X_e1` → 正確 (d) merge phrases 含 `_e` 上游 → 正確 (e) 不含 `_e` 的既有閘道 import 行為不變。',
      '**動到的檔案（2 個）**：`src/model/connectionFormat.js`（NUM / NUM_HEAD / RE_LOOP_LEGACY 三處 regex 加 `_e\\d*`）/ `src/data/changelog/current.js`（本條）。',
    ],
  },
  {
    date: '2026-05-05',
    title: 'PR-D6：修 Excel 匯入 `_e` shapeType 被洗掉的串連 bug + FlowTable 紅框改紅字',
    items: [
      '**緣由**：使用者回報「匯入 _e 後綴編號的會被認為是 L4 任務並畫紅框」+「同角色同時有任務和 _e，被認為是外部角色了」。Trace 後發現是兩個 bug 串連觸發，加上規則 6「表格紅框移除、改成 L4 編號變紅色」。',
      '**Bug 1（核心）**：`utils/taskDefs.js normalizeTask` 第 170 行 `let shapeType = "task"` — 永遠 default 重設，line 173 只認舊 model 的 `task.type === "interaction"`，不認新 model 的 `task.shapeType === "interaction"`。Excel 進來的 `_e` row 是 `type="task" shapeType="interaction"`（PR #111 後新模型），FlowEditor 開啟時 `normalizeTask` 跑過 → shapeType 從 `interaction` 被重設成 `task`。**修法**：line 170 改 `let shapeType = task.shapeType || "task"`（preserve existing），下方 type-specific 分支仍可覆寫（legacy `type="interaction"` migration 還是過得去）。',
      '**Bug 2（連動）**：`excelImport.buildFlow` line 140 `...(isInteraction && taskType === "task" ? { shapeType: "interaction" } : {})` — 一般 task 沒設 shapeType key（undefined）。`detectRoleTypes` line 276 `if (t.shapeType !== "task" && t.shapeType !== "interaction") return;` 跳過 undefined → 一般 task 全部不算數，混用角色被誤判為「全 interaction」→ 推為 external。**修法**：buildFlow 對所有 `taskType === "task"` 顯式設 `shapeType`（task 或 interaction），不留 undefined。',
      '**串連劇本**：(1) detectRoleTypes 漏掉一般 task → 角色推為 external (2) applyExternalPrefixToRoles 補 `[外部角色]` 前綴 (3) FlowEditor 開啟 → normalizeTask 把 `_e` row shapeType 從 interaction 洗成 task (4) external lane + shapeType=task → getLaneShapeViolations 命中 → 紅框出現在原本的 `_e` row 上。Bug 1 修完 (3) 不再發生；Bug 2 修完 (1) 不再發生。兩個都得修，獨立修任一個都會留另一個 bug。',
      '**規則 6（FlowTable 紅框 → 紅字）**：使用者要求「表格的紅框移除，改成 L4 任務編號那欄的編號變成紅色即可」。`FlowTable.jsx ReadCell` 加 `danger` prop（紅 + bold），`tasks.map` 的 `<tr>` 拿掉 `outline outline-red-500`，改在 L4 編號 ReadCell 傳 `danger={isViolation}` + `title=...`。流程圖紅框（PR-D3）保持不變。',
      '**驗證**：`npm run build` 通過。功能驗證點：(a) Excel 匯入「全 _e」角色 → 推 external + 補前綴（PR-D5 行為，Bug 2 修完才正確） (b) Excel 匯入「混用 task + _e」角色 → 推 internal、_e row 流程圖紅框 + 表格 L4 編號紅字 (c) 進編輯器 _e row shapeType 維持 interaction（不被 normalizeTask 洗掉） (d) 規則 1「`_e` 必為外部互動」end-to-end 守住。',
      '**動到的檔案（5 個）**：`src/utils/taskDefs.js`（normalizeTask preserve shapeType）/ `src/utils/excelImport.js`（buildFlow 顯式 shapeType）/ `src/components/FlowTable.jsx`（ReadCell danger prop）/ `docs/business-spec.md` §3.1（紅框 → 紅字描述）/ `src/data/helpPanelData.js`（VALIDATION 同步）/ `src/data/changelog/current.js`（本條）。',
    ],
  },
  {
    date: '2026-05-05',
    title: 'PR-D5：Excel 匯入智慧推導角色類型（純看 row 分布、不讀「角色類型」欄）',
    items: [
      '**緣由**：使用者規則 7 + 8 + E&G — Excel 匯入時依該角色 row 的元件分布自動判斷 internal / external，不依賴 Excel 任何「角色類型」欄位。完成「外部互動 / 外部角色重構」最後一支 PR。',
      '**`utils/excelImport.js detectRoleTypes`** 新增純函式：walk lane-sensitive tasks（type=task + shapeType in {task, interaction}），按 roleId 累計 task / interaction 計數，套規則：(a) 全 interaction → external (b) 全 task / 混用 → internal (c) 沒任何 lane-sensitive row → internal（預設）。混用 case 中 `_e` row 仍是 interaction shape，PR-D3 紅框會在 internal lane 上 surface 給使用者檢查。',
      '**`buildFlow` return**：在 `applyExternalPrefixToRoles` 之前先跑 `detectRoleTypes`，讓被推為 external 的角色在前綴自動化時自然帶上「[外部角色]」（兩個 PR 串聯一起）。',
      '**`docs/business-spec.md §3.1`**：Excel 匯入段擴成完整對照表（4 種 row 分布 → role.type 推論 + 備註）。明確標註「不再讀 Excel 角色類型欄」。',
      '**`src/data/helpPanelData.js` VALIDATION import 區**：第一條新增「角色類型自動推導」說明 5 種情境推論結果，給使用者了解匯入後角色泳道為何會是某 type。',
      '**整個 D 系列完成（PR-D1 → D5）**：',
      '  • **D1 (#159)**：`_w → _e` 後綴 rename + Excel 嚴格 `_e`',
      '  • **D2 (#160)**：lane↔shape sync 對稱化（internal lane 也強制 task）',
      '  • **D3 (#161)**：違規元件紅框 UI（流程圖 + FlowTable）+ PNG / drawio 排除',
      '  • **D4 (#162)**：`[外部角色]` 前綴自動化（onBlur / 切 type / Excel / load）+ 兜底 warning',
      '  • **D5 (本 PR)**：Excel role.type 智慧推導（純看 row 分布）',
      '**驗證**：`npm run build` 通過。功能驗證點：(a) Excel 全 `_e` 角色 → 推論 external + 自動補前綴 (b) 全 task 角色 → 推論 internal (c) 混用角色 → 推論 internal、`_e` row 紅框警示 (d) 只有 start / end / gateway 角色 → 預設 internal (e) PR-D4 onBlur 在 Excel 匯入後 Wizard / Drawer 改名仍正常運作。',
      '**動到的檔案（4 個）**：`src/utils/excelImport.js`（detectRoleTypes + buildFlow 串接）/ `docs/business-spec.md` §3.1（Excel 匯入推導表）/ `src/data/helpPanelData.js`（VALIDATION import 條目）/ `src/data/changelog/current.js`（本條）。',
    ],
  },
  {
    date: '2026-05-05',
    title: 'PR-D4：外部角色名稱「[外部角色]」前綴自動補 + 儲存兜底檢核',
    items: [
      '**緣由**：使用者規則 5「外部角色以『[外部角色]角色名稱』呈現」+ 規則 6「儲存時若沒前綴跳提醒」+ 規則 I「使用者刪掉前綴自動補回」。本 PR 把前綴自動化跨所有寫入路徑（建立 / 改 type / 改名 onBlur / Excel / localStorage 載入），validation 加兜底警示。',
      '**`utils/elementTypes.js`** 新增 `EXTERNAL_ROLE_PREFIX = "[外部角色]"` 常數 + `ensureExternalPrefix(role)` 純函式（idempotent — 已前綴或非 external 都回 same ref）+ `applyExternalPrefixToRoles(roles)` cascade helper（同 ref 短路）。',
      '**`Wizard.jsx` Step 2 角色設定**：(a) `updateRole(id, "type", "external")` 觸發後立即套 `ensureExternalPrefix`（規則 5 切到外部即補）(b) 角色名 input 加 `onBlur={onRoleNameBlur}` — 失焦時若是 external 且缺前綴自動補回（規則 I）(c) `role.type === "internal"` 時不動，使用者可自由命名。',
      '**`FlowEditor/DrawerContent.jsx` 角色泳道列表**：同 Wizard 的 onBlur + 切外部時補前綴邏輯。Drawer / Wizard 兩套入口同步處理。',
      '**`utils/excelImport.js buildFlow`**：return 時包 `applyExternalPrefixToRoles(roles)`。當前 Excel 解析端 `role.type` 全標 internal（PR-D5 才會偵測），此呼叫先預埋；PR-D5 設 external 時前綴會自然 follow。',
      '**`utils/storage.js migrateFlow`**：load 時 `applyExternalPrefixToRoles(flow.roles)`，把舊 localStorage 資料的外部角色補上前綴。Idempotent — 已前綴的 same-ref 短路。',
      '**`model/validation.js` rule 6b**：external 角色名沒「[外部角色]」開頭跳 warning + 名字只剩前綴（empty payload）也跳 warning。常規路徑下使用者看不到（自動補完已涵蓋）— 兜底防程式化匯入 / 未來 JSON import 等繞過 UI 的路徑。',
      '**驗證**：`npm run build` 通過。功能驗證點：(a) Wizard 切「外部角色」→ 名稱自動加「[外部角色]」(b) 改名刪掉前綴 → onBlur 自動補回 (c) DrawerContent 同樣行為 (d) external + 空 payload（單純「[外部角色]」）儲存時跳 warning (e) Excel 載入後角色 type 仍 internal（PR-D5 前），前綴邏輯 no-op — 非 regression。',
      '**動到的檔案（8 個）**：`src/utils/elementTypes.js`（helper）/ `src/components/Wizard.jsx`（onBlur + cascade）/ `src/components/FlowEditor/DrawerContent.jsx`（onBlur + cascade）/ `src/utils/excelImport.js`（buildFlow return）/ `src/utils/storage.js`（migrateFlow）/ `src/model/validation.js`（rule 6b）/ `src/data/helpPanelData.js`（VALIDATION 新條目）/ `docs/business-spec.md` §7.2（前綴 warning）/ `src/data/changelog/current.js`（本條）。',
      '**接下來 PR-D5**：Excel 智慧角色 type 偵測（規則 7/8 — `_e` 多數 → external、混用 → internal + 紅框讓使用者修）。前綴 prefix 邏輯已備妥，PR-D5 設 type 時自動補。',
    ],
  },
  {
    date: '2026-05-05',
    title: 'PR-D3：lane / 元件 shape 違規紅框警示（含 FlowTable + PNG / drawio 排除）',
    items: [
      '**緣由**：PR-D2 把 lane↔shape sync 改為對稱、且 load-time 不再強制 cascade — 既有違規（內部泳道的 interaction、外部泳道的 task）需要視覺方式提醒使用者。本 PR 落地紅框 UI（規則 9 + backlog AJ 配套）。',
      '**`model/flowSelectors.js`** 加 `getLaneShapeViolations(tasks, roles)` — 純函式回傳違規 task id Set。內部泳道 + interaction 或外部泳道 + task 命中違規；start / end / gateway / l3activity / role-less task 一律放行。同一份 SOT 餵流程圖跟 FlowTable，避免兩處邏輯各寫各的（吃過 buildTableL4Map vs displayLabels 的虧）。',
      '**`DiagramRenderer/shapes.jsx TaskShape`** 加 `isViolation` prop — 違規時在原本矩形外圍多畫一層 `<rect>` red overlay（紅 `#EF4444`、stroke 2.5、`pointerEvents="none"` 不擋滑鼠、外擴 1.5px 讓邊框可見）。**附 `data-export-skip="1"` 屬性**。`pointerEvents="none"` 確保 hover / click 不被搶掉。',
      '**`DiagramRenderer/TasksLayer.jsx`** 接 `violationIds` prop 透傳到 TaskShape；`DiagramRenderer/index.jsx` 在 render 階段呼叫 `getLaneShapeViolations(flow.tasks, flow.roles)` 算出 set 傳下去。',
      '**`DiagramRenderer/index.jsx`** 加 `pngExportFilter` module-level helper：`node => !(node?.dataset && node.dataset.exportSkip === \'1\')`。傳給 `toPng` 兩個觸發點（auto-export `useEffect` + 手動 `handleExportPng`）的 options。drawio 不需要動 — `drawioExport.js` 自己構造 XML，本來就不會輸出 React-rendered 紅框 overlay。',
      '**`FlowTable.jsx` (backlog AJ)**：對應 row 加 `outline outline-2 -outline-offset-2 outline-red-500` Tailwind classes + `title` tooltip 描述違規原因。同樣呼叫 `getLaneShapeViolations` 計算 violationIds（`useMemo` deps `[tasks, flow.roles]`），跟流程圖紅框完全同步。',
      '**`docs/business-spec.md §3.1`**：標題從「不對稱」改為「對稱」、Sync 規則表格擴成 6 row（含 internal cascade / 載入舊資料保留 / 手動切換）+ 新增「紅框警示」段落明確 PNG / drawio 排除機制 + 對應實作 list 更新。',
      '**`src/data/helpPanelData.js`**：ELEMENTS「外部關係人互動」purpose 條目改成 enforced 雙向強制、明確紅框警示與匯出排除；VALIDATION 對應條目重寫為「泳道角色類型與元件形狀必須對應（雙向強制）」涵蓋 6 個 detail 子點。',
      '**驗證**：`npm run build` 通過；bundle 從 945KB → 947KB（純 helper 函式 + JSX 條件 render，純函式可以 tree-shake friendly）。功能驗證點：(a) 內部泳道任務手動選「外部互動」型 → 元件外圍紅框 + FlowTable row 紅框 (b) 角色從外部翻內部 → cascade 成 task，紅框消失 (c) PNG 匯出 → 紅框不出現 (d) drawio 匯出 → 紅框不出現 (e) 違規元件可正常 hover / click（pointerEvents none 保留交互）。',
      '**動到的檔案（7 個）**：`src/model/flowSelectors.js`（new selector）/ `src/components/DiagramRenderer/{shapes,TasksLayer,index}.jsx`（紅框 overlay + filter）/ `src/components/FlowTable.jsx`（row 紅框）/ `docs/business-spec.md`（§3.1 改寫）/ `src/data/helpPanelData.js`（ELEMENTS + VALIDATION）/ `.claude/backlog.md`（AJ 移除）/ `src/data/changelog/current.js`（本條）。',
      '**接下來**：PR-D4 `[外部角色]` 前綴自動補 + 儲存檢核（規則 5 / 6 / I）/ PR-D5 Excel 智慧角色 type 偵測（規則 7 / 8）。',
    ],
  },
  {
    date: '2026-05-05',
    title: 'PR-D2：外部互動 / 外部角色對稱 cascade（internal lane → task 也強制）',
    items: [
      '**緣由**：PR-D1 完成 `_w → _e` rename 後，使用者進一步明確：外部角色泳道「不能用任務」、內部角色泳道「不能用外部互動」（規則 2 + 4）。原本 `targetShapeFor` 只強制 external→interaction（不對稱），internal lane 允許 interaction（由 validation 3e 跳 warning）— 不符合新規格。本 PR 切到對稱規則：兩邊 lane 都強制對應 shape，cascade 在使用者顯式切換 role.type / roleId 時觸發。',
      '**`utils/elementTypes.js` `targetShapeFor`**：external → `interaction` / **internal → `task`**（新增）/ unspecified → preserve。註解更新為「Symmetric strict rule」。`applyRoleChange` / `syncTasksToRoles` 邏輯不變（純函式），自動套用對稱規則 — strip `l4Number` 讓 `computeDisplayLabels` 重推（`_e` ↔ regular L4）。',
      '**`utils/storage.js` `migrateFlow`**：拿掉 load-time 的 `syncTasksToRoles` 呼叫（連同 import）。原本是給「不對稱時代」的 fixup（避免 external lane 殘留 task），現在改成讓既有違規以「紅框警示」方式 surface（PR-D3 會落地紅框 UI），而非 silent auto-convert。Cascade 仍保留在使用者顯式編輯 role.type / roleId 的觸發點（Wizard / DrawerContent / TaskCard / ContextMenu）。',
      '**`model/validation.js` rule 3e**：對稱化警示文字 — internal lane + interaction 與 external lane + task 雙向偵測，均跳 warning（仍可儲存）。原訊息「建議改放外部角色泳道」改為「畫面上有紅框」（前指 PR-D3）— 文字提示與紅框視覺 reinforce。',
      '**Edge case 行為**：(F) 載入舊 localStorage 資料若有違規 → 不自動修，保持資料原貌等使用者決定（紅框 in PR-D3）(B) external→internal cascade 後 `_e1/_e2/_e3` 重編成正式 L4 順號 → 後續任務編號自然 shift，使用者已確認接受 (C) TaskCard 元件類型 picker 仍允許自由選擇（不擋）— 違規由紅框提醒。',
      '**驗證**：`npm run build` 通過，bundle size 不變。隔離保證 — `applyRoleChange` / `syncTasksToRoles` 都是純函式，存量 callers 不需要改動（觸發點 audit 確認 Wizard / DrawerContent / TaskCard / ContextMenu 走相同 API）。',
      '**動到的檔案（4 個）**：`src/utils/elementTypes.js`（targetShapeFor 對稱）/ `src/utils/storage.js`（migrateFlow drop syncTasksToRoles）/ `src/model/validation.js`（rule 3e 對稱）/ `src/data/changelog/current.js`（本條）。',
      '**接下來**：PR-D3 加紅框違規顯示（含 PNG / drawio export 過濾） + FlowTable 紅框（backlog AJ）/ PR-D4 `[外部角色]` 前綴自動補 + 儲存檢核（規則 5/6/I）/ PR-D5 Excel 智慧角色 type 偵測（規則 7/8）。',
    ],
  },
  {
    date: '2026-05-05',
    title: '外部關係人互動編號後綴 `_w` → `_e` 全 codebase rename + 凍結 c24',
    items: [
      '**緣由**：使用者：「外部互動、外部角色重構」— 2026-04-30 引入時用 `_w`（取自 external 一字「w」rld 的隨意命名），但 `_e` 更直觀對應 external interaction，且跟其他後綴（`_g` gateway / `_s` subprocess）的字母縮寫邏輯一致。趁輔助欄位 PR-A/B/C 落地後 codebase 相對穩定，把 `_w` 全面改名為 `_e`，避免日後越積越多 reference 難改。',
      '**Step 1 — Regex SOT（`utils/taskDefs.js`）**：`L4_NUMBER_PATTERN` / `L4_INTERACTION_PATTERN` 從 `_w\\d*` 改為 `_e\\d*`。註解明確標記「Renamed from `_w` to `_e` in 2026-05-05」+ legacy migration 入口參考。',
      '**Step 2 — Display label SOT（`model/flowSelectors.js`）**：`computeDisplayLabels` interaction branch 全套改 `_e`（counter `intConsec` / regex `/^(\\d+-\\d+-\\d+-\\d+)_e(\\d*)$/` / strip pattern `(_g\\d*|_s\\d*|_e\\d*)$` / output `${base}_e${intConsec}` / post-process run-length-1 → drop index）。`_e1 → _g → _e2` 連續性與 `_w` 時代行為一致。',
      '**Step 3 — Excel I/O（`utils/excelImport.js`）**：parser 嚴格只接受 `_e`（`isInteraction = /_e\\d*$/`）；`validateNumbering` 錯誤訊息與輔助範例改用「外部互動為 1-1-1-1_e」；前綴對應檢查 strip pattern 同步加 `_e\\d*`。**舊 `_w` Excel 檔不再被接受**（會在 `validateNumbering` 階段以「格式錯誤」擋下）— 想匯入舊檔請先用 localStorage 載入觸發 migration、再重新匯出。',
      '**Step 4 — Migration（`utils/storage.js`）**：`migrateInteractionSuffix` 強化為兩段式 — (a) 既有「`shapeType=interaction` 但無後綴」的 pre-2026-04-30 資料 strip 重推 (b) 既有 `_w\\d*$` 後綴的 2026-04-30~2026-05-04 資料同樣 strip 讓 `computeDisplayLabels` 重推為 `_e`。Idempotent — 已是 `_e` 的 task 不動。',
      '**Step 5 — UI / 顯示**：`DiagramRenderer/TasksLayer.jsx` hide regex `(_g\\d*|_s\\d*|_e\\d*|-0|-99)`、`utils/elementTypes.js` 註解與 `applyRoleChange` / `syncTasksToRoles` 的 strip 邏輯註解全更新、`model/validation.js` rule 3e 註解與 displayLabels 範例註解同步改 `_e`。',
      '**Step 6 — 文件三件組同步**：(a) `docs/business-spec.md §2.1 / §2.4 / §2.5 / §3 / §3.1` 編號表 / 連續性 / 字母結尾例外 / 元件表 / 不對稱 sync 章節全改 `_e` — 章節標題「外部關係人互動 — 不對稱 sync + `_e` 編號」 (b) `src/data/helpPanelData.js` NUMBERING / VALIDATION / ELEMENTS 含 `_w` 條目全改 (c) `CLAUDE.md §3` 三後綴 family 共用 anchor 規則範例改用 `_e`。',
      '**Step 7 — 凍結 c24**：current.js 累積到 76KB（PR #153 後沒再 freeze），遠超 7KB 軟上限。`current.js` → `c24.js` rename（內含 PR #153 / #154 / #155 / #156 / #157 / #158），`current.js` reset 成本條，`index.js` 加 c24 import。',
      '**驗證**：`npm run build` 通過，bundle size 不變（rename 不增邏輯）。grep audit 確認 src/ 內 `_w` 只剩 (a) `storage.js` 既有 legacy 偵測 regex (b) `taskDefs.js` 註解描述 rename 歷程 — 流程邏輯本身已完全切到 `_e`。',
      '**動到的檔案（11 個）**：`src/utils/taskDefs.js`（regex）/ `src/utils/storage.js`（migration 強化）/ `src/utils/excelImport.js`（嚴格 `_e`）/ `src/utils/elementTypes.js`（註解）/ `src/model/flowSelectors.js`（display label SOT）/ `src/model/validation.js`（註解）/ `src/components/DiagramRenderer/TasksLayer.jsx`（hide regex）/ `src/data/helpPanelData.js`（user-facing 文字）/ `docs/business-spec.md`（§2.1 / §2.4 / §2.5 / §3 / §3.1）/ `CLAUDE.md §3`（三後綴 family 範例）/ `src/data/changelog/current.js`（freeze + 本條）+ 新檔 `src/data/changelog/c24.js`（freeze 過去條目）/ `src/data/changelog/index.js`（加 c24 import）。',
    ],
  },
];
