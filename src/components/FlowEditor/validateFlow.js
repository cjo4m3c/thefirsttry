// `validateFlow` lives in `src/model/validation.js` (PR-7).
// Re-exported here so the existing FlowEditor/index.jsx importer keeps
// working without changes.
export { validateFlow } from '../../model/validation.js';
